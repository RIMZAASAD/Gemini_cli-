```
simple-calculator/
├── .venv/
├── app.py
├── main.py
└── pyproject.toml
```

This project contains a simple calculator implemented in Python, with a command-line interface (`main.py`) and a Streamlit web interface (`app.py`). It uses `uv` for dependency management.

link project 

https://gyaznevgtxxwcba8wlmt2w.streamlit.app/
